# Yokko 协作约定

## 沟通与执行

- 默认使用中文沟通，先理解现有实现再修改。
- 尊重工作区已有改动，不覆盖或清理与当前任务无关的内容。
- 优先完成最小、清晰、可验证的改动；未经要求不要顺手大规模重构。

## 子代理协作

- 横跨多目录的代码定位、上游实现盘点、长日志和测试输出归纳、机械批量修改、格式整理及 focused build/test 等脏活累活，默认交给 Luna；主代理保留模块边界、行为语义、风险判断和最终验收。
- 只读搜证使用自定义 `luna_explorer`，明确文件所有权后的实现工作使用自定义 `luna_worker`；不要使用可能解析为 Terra 的内置 `explorer` 或 `worker`。若当前任务没有加载这两个 Luna 角色，就由主代理直接完成，不得回退到 Terra。派发任务必须给出限定模块或文件、现有脏改提醒、预期交付物与验证范围；子代理不得继续派生。
- 独立任务使用精简的自包含上下文，通常最多并行 2 个互不重叠的任务。禁止重复搜索、重叠写文件、无边界全库扫描，以及把完整构建日志或大段源码带回主线程。
- 单文件小改、强耦合修改和需要架构取舍的工作由主代理直接完成。涉及 ASIO/WASAPI 设备、真实播放、实机交互或破坏性操作时，不得仅因交给子代理而扩大授权。
- 子代理回报应只有结论、准确的 `file:line`、修改文件、focused 验证和剩余风险；主代理在证据无冲突时直接整合，避免为“确认”重复消耗 token。

## 模块化原则

Yokko 必须保持模块化，但模块化服务于可理解、可维护和可测试，不追求形式上的绝对纯粹。

- `Yokko.Core`：领域模型、谱面、时间轴、玩法规则、计分等与 UI 和平台无关的核心逻辑。
- `Yokko.Audio`：音频抽象、播放能力和音频基础设施，不承载界面状态。
- `Yokko.Import`：外部格式解析、转换和导入流程；通用领域结果落到 `Yokko.Core`。
- `Yokko.Game`：共享的应用层、界面、交互和 osu!framework 集成，可组合 Core、Audio、Import。
- `Yokko.Desktop`、`Yokko.iOS`：平台入口、启动配置及平台专属适配，业务逻辑不得长期堆在入口层。
- `Yokko.Resources`：共享资源及资源访问相关内容。
- `Yokko.Game.Tests`：可重复执行的自动化测试，优先覆盖核心行为和回归风险。

新增功能先放进职责最接近的现有模块。只有在出现清晰、稳定且可独立描述的职责边界时，才新增项目或顶层模块。

## 上游参考实现

AI 在设计或实现功能前，可以主动搜索、阅读和对照以下成熟开源项目，不必闭门重新发明已有方案：

- [osu!framework](https://github.com/ppy/osu-framework)：优先参考框架生命周期、渲染、输入、音频、平台抽象、依赖注入和测试方式。
- [osu! / osu!lazer](https://github.com/ppy/osu)：优先参考音游架构、谱面模型、玩法逻辑、界面交互、资源管理及跨平台实现。
- [Etterna](https://github.com/etternagame/etterna)：优先参考键盘音游玩法、判定与时序、滚速、谱面处理、输入和性能优化。

允许直接复用、移植甚至照抄其中已经成熟的实现，不要求为了“原创”重复造轮子，但必须做到：

- 先确认参考代码解决的是同一类问题，并适配 Yokko 当前使用的语言、框架和模块边界。
- 复制或大段移植时，在代码注释或相关文档中记录原仓库、文件路径和参考版本或 commit，方便后续追踪。
- 保留并遵守原项目许可证、版权声明及资源许可要求；不要复制不属于开源许可范围的品牌素材或受限资源。
- 不把上游内部结构原封不动堆进一个文件；移植后仍应落入 Yokko 对应的 Core、Audio、Import、Game 或平台模块。
- 对关键行为补充 focused tests，确认结果符合 Yokko 的需求，而不只是与上游代码长得一样。
- 若多个上游方案不同，优先选择最贴合 Yokko 当前架构、最容易维护和验证的方案，并简要说明取舍。

## 视觉素材与界面实现

- Yokko 的共享逻辑布局基准为 `1920×1080`。视觉稿、image-to-code 坐标和同视口实机对照默认直接使用这一基准；不得再按旧 `1600×900` 基准新增全屏界面。已有组件若保留独立内部画布，应明确标注并在外层适配共享基准。
- 实现视觉稿前先区分“界面结构”和“美术素材”。布局、动态文字、状态和交互由代码实现；插画、贴纸、手绘边框、纹理、复杂装饰轮廓等美术内容应使用真实素材。
- AI 已获准使用图像编辑能力，从用户提供或许可明确的视觉稿中直接抠取界面所需的独立美术部件，无需再次询问是否允许抠图。应按部件分别导出透明 PNG，保留原始比例与清晰度，检查并清理白边、黑边、残留背景和半透明脏边；不得把包含动态文字或交互状态的整张界面截图当作单一素材使用。
- 面对不规则、插画化或明显依赖美术风格的元素，不要用 `Box`、`Circle`、`Triangle`、字符、Emoji 或大量基础图形硬拼近似效果，也不要因为代码能画就默认用代码重画。
- 视觉稿中缺少可复用素材时，优先按以下顺序处理：复用项目已有素材；编辑已有源素材；根据选定视觉稿生成独立的高质量位图素材。需要透明背景时，可以先生成纯色键控背景，再可靠地移除背景并检查透明边缘。
- 生成或提取的正式素材必须放入 `Yokko.Resources` 并由实际界面加载，不能只留在临时目录或用整张效果图冒充界面。需要本地化、会变化或可交互的文字仍应由代码叠加在素材上。
- 简单面板、分割线、规则几何装饰以及项目图标库已有的标准图标可以继续使用代码绘制。判断标准是能否忠实还原，而不是实现方式看起来是否“更程序化”。
- 有选定视觉稿时，应以相同视口和状态截取实机画面并并排比较；若轮廓、比例、层次或质感明显不同，不得仅凭“元素都有”就认为完成。

## 依赖与边界

- 依赖方向保持清楚：平台入口依赖 `Yokko.Game`；`Yokko.Game` 组合基础模块；基础模块不要反向依赖 UI 或平台入口。
- 跨模块通过小而明确的公共 API 协作，避免读取对方内部状态、复制同一份规则或建立循环依赖。
- 平台差异留在平台项目或适配层；领域逻辑不能依赖 Windows、iOS 或具体窗口实现。
- UI、音频、导入、领域计算各自保有明确职责。一个类若同时承担多类职责，应拆分或抽出协作者。
- 公共状态要有单一真源。不要为了方便在多个模块维护会漂移的副本。

## 不要过度设计

以下情况可以务实处理，不强制拆分：

- 很小且只在一个位置使用的辅助逻辑，可以留在调用方附近。
- 简单数据传递不必专门引入 service、manager、factory 或 interface。
- 没有第二个真实实现、测试替身或明确隔离收益时，不要求“每个类都配接口”。
- 不按固定行数、文件数或类数量机械拆分；以职责是否混杂、变化原因是否不同为判断标准。
- 不为尚未出现的需求提前建立插件系统、通用框架或多层抽象。

当局部实现开始被多个功能复用、包含独立业务规则、需要单独测试，或频繁与调用方一起膨胀时，再提取为独立类型或模块。

## 代码组织

- 按功能和领域组织目录，名称应直接表达职责，避免 `Utils`、`Helpers`、`Managers` 逐渐成为杂物箱。
- 单个文件尽量只表达一个主要概念；紧密相关的小类型可以放在一起。
- 优先组合而非深继承，优先显式依赖而非全局可变状态。
- 新增公共 API 时保持最小暴露面；能用 `internal` 就不要无理由设为 `public`。
- 修改模块边界或项目引用时，同步更新相关文档和测试。

## 验证要求

- 小改动做与改动直接相关的快速检查即可，不默认跑耗时的全量测试。
- 隔离构建、测试、发布和上游参考仓库不得写入 `%TEMP%` 或 C 盘；统一使用 `F:\YokkoArtifacts` 下按用途命名的可复用目录，任务完成后删除不再需要的临时副本，避免为每次验证重复保留整套构建输出。
- 修改核心规则、音频链路、导入转换、项目依赖或跨模块接口时，运行对应构建和 focused tests。
- 大改、跨模块重构或发布前变更必须进行完整构建与相关测试。
- 如果受环境或依赖限制无法完成验证，要明确说明已验证内容、未验证内容和剩余风险。
