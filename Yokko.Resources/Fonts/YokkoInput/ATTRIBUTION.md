# YokkoInput font atlas

`YokkoInput` is a regular-weight bitmap subset generated from Chill Round
Gothic for user-entered settings search text.

It uses the same pinned upstream font and SIL Open Font License as the sibling
`Fonts/Yokko` assets. See `../Yokko/ATTRIBUTION.md` and `../Yokko/OFL.txt`.
